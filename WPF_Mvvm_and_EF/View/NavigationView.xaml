<UserControl x:Class="WPF_Mvvm_and_EF.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:WPF_Mvvm_and_EF.Model;assembly=WPF_Mvvm_and_EF.Model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding Friends}"
                  SelectedItem="{Binding selectedFriend,Mode=TwoWay}" >
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type model:LookupItem}">
                    <TextBlock Text="{Binding DisplayMember}"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
            
    </Grid>
</UserControl>
